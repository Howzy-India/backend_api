# Howzy — Remaining Features Implementation Plan

## Goal
Autonomously develop, test, and merge all remaining features with zero human intervention. Report success when all 11 e2e specs pass and all PRs are merged.

## Status

| Feature | Spec | Tests | PR | Merged |
|---|---|---|---|---|
| Auth | 01 | ✅ 4/4 | #9 | ✅ |
| Admin Users | 02 | ✅ 4/4 | #13 | ✅ |
| Client Logins | 03 | ✅ 3/3 | #13 | ✅ |
| Client Portal Nav | 04 | ✅ 4/4 | #14 | ✅ |
| Property Listings | 05 | ✅ 6/6 | #15 | ✅ |
| **Enquiries** | 06 | 🔲 | — | — |
| **Leads** | 07 | 🔲 | — | — |
| **Client Browse/Enquire** | 08 | 🔲 | — | — |
| **Pilot Dashboard** | 09 | 🔲 | backend needed | — |
| **Partner Dashboard** | 10 | 🔲 | backend needed | — |
| **Verification + Attendance** | 11 | 🔲 | — | — |

## Backend PRs Needed

### backend_api PR: `feat/admin-create-user-with-role`
- `POST /admin/create-user` — creates user with ANY role (super_admin only)
  - Body: `{ email, password, displayName, role: 'agent'|'partner'|'client'|'admin' }`
  - Creates Firebase Auth user + sets custom claim + writes Firestore users doc
- `DELETE /admin/users/:uid` — extend to handle any non-super_admin user (remove role === 'admin' restriction)
- **Required before**: F-09 (Pilot), F-10 (Partner)

## Frontend PRs Needed

### frontend-web PR: `feat/enquiries-e2e` (F-06)
- `e2e/06-enquiries.spec.ts`
- Seed an enquiry in beforeAll, assert table loads, filter works, timeline visible
- Cleanup enquiry in afterAll (need `deleteEnquiry` helper or skip cleanup if seeded enquiries don't pollute)

### frontend-web PR: `feat/leads-e2e` (F-07)
- `e2e/07-leads.spec.ts`
- Seed a lead via `POST /leads`, assert leads tab loads, lead allocation tab visible
- Cleanup lead in afterAll

### frontend-web PR: `feat/client-browse-enquire` (F-08)
- `e2e/08-client-portal-browse.spec.ts`
- Client login → Projects tab → property visible → submit enquiry → Client Dashboard shows it
- Uses seeded property from F-05 pattern

### frontend-web PR: `feat/pilot-dashboard-e2e` (F-09)
- `e2e/09-pilot-dashboard.spec.ts`
- beforeAll: create agent user via `POST /admin/create-user`
- Test: login as agent → Listed Projects, Leads, Earnings tabs visible
- afterAll: delete agent user

### frontend-web PR: `feat/partner-dashboard-e2e` (F-10)
- `e2e/10-partner-dashboard.spec.ts`
- beforeAll: create partner user via `POST /admin/create-user`
- Test: login as partner → Dashboard tab, Assigned Leads tab
- afterAll: delete partner user

### frontend-web PR: `feat/verification-attendance-e2e` (F-11)
- `e2e/11-verification-attendance.spec.ts`
- Super admin → Verification Panel tab visible
- Super admin → Attendance tab visible
- No seeding needed

## Execution Sequence

1. ✅ Update features.md + skills.md (DONE)
2. → Implement backend `feat/admin-create-user-with-role` → PR → merge → wait deploy
3. → F-06 (Enquiries): write spec → test → fix → PR → SonarCloud → merge
4. → F-07 (Leads): write spec → test → fix → PR → SonarCloud → merge
5. → F-08 (Client Browse): write spec → test → fix → PR → SonarCloud → merge
6. → F-09 (Pilot): write spec → test (uses new backend) → fix → PR → SonarCloud → merge
7. → F-10 (Partner): write spec → test → fix → PR → SonarCloud → merge
8. → F-11 (Verification): write spec → test → fix → PR → SonarCloud → merge
9. → Report: "All 11 specs passing, all PRs merged. Platform complete ✅"

## SonarCloud Rules to Follow
- Never hardcode credentials — always `process.env['E2E_*']`
- Extract ALL repeated helpers to `e2e/helpers.ts`
- No copy-pasted sign-in / navigation blocks across specs
- New spec files must stay < 3% duplication relative to other files
